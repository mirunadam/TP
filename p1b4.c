/*Implement a C program that reads from a file (with the path given as command line argument) a list of up
to 100 key value pairs written as key=value, one per line and prints in ascending lexicographical order
all the values ordered by key, breaking ties by value.*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>



int main(){

    return 0;
}